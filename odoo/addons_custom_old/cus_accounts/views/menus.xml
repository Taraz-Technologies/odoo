<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <data>
<!--        Payment Invoices-->
        <record id="action_move_out_invoice_type_payment" model="ir.actions.act_window">
            <field name="name">Payment Invoices</field>
            <field name="res_model">account.move</field>
            <field name="view_mode">tree,kanban,form,pivot,graph,activity</field>
            <field name="view_id" ref="account.view_invoice_tree"/>
            <field name="search_view_id" ref="account.view_account_invoice_filter"/>
            <field name="domain">[('type', '=', 'out_invoice'), ('journal_id', 'in', (25, 90))]</field>
            <field name="context">{'default_type': 'out_invoice'}</field>
            <field name="help" type="html">
              <p class="o_view_nocontent_smiling_face">
                Create a customer invoice
              </p><p>
                Create invoices, register payments and keep track of the discussions with your customers.
              </p>
            </field>
        </record>

        <menuitem id="menu_action_move_out_invoice_type_payment"
                  action="action_move_out_invoice_type_payment"
                  parent="account.menu_finance_receivables"
                  sequence="1"/>

<!--        Delivery Invoices-->
        <record id="action_move_out_invoice_type_delivery" model="ir.actions.act_window">
            <field name="name">Delivery Invoices</field>
            <field name="res_model">account.move</field>
            <field name="view_mode">tree,kanban,form,pivot,graph,activity</field>
            <field name="view_id" ref="account.view_invoice_tree"/>
            <field name="search_view_id" ref="account.view_account_invoice_filter"/>
            <field name="domain">[('type', '=', 'out_invoice'), ('journal_id', 'in', (112, 114))]</field>
            <field name="context">{'default_type': 'out_invoice'}</field>
            <field name="help" type="html">
              <p class="o_view_nocontent_smiling_face">
                Create a customer invoice
              </p><p>
                Create invoices, register payments and keep track of the discussions with your customers.
              </p>
            </field>
        </record>

        <menuitem id="menu_action_move_out_invoice_type_delivery"
                  action="action_move_out_invoice_type_delivery"
                  parent="account.menu_finance_receivables"
                  sequence="1"/>

<!--        Service Invoices-->
        <record id="action_move_out_invoice_type_service" model="ir.actions.act_window">
            <field name="name">Service Invoices</field>
            <field name="res_model">account.move</field>
            <field name="view_mode">tree,kanban,form,pivot,graph,activity</field>
            <field name="view_id" ref="account.view_invoice_tree"/>
            <field name="search_view_id" ref="account.view_account_invoice_filter"/>
            <field name="domain">[('type', '=', 'out_invoice'), ('journal_id', '=', 115)]</field>
            <field name="context">{'default_type': 'out_invoice'}</field>
            <field name="help" type="html">
              <p class="o_view_nocontent_smiling_face">
                Create a customer invoice
              </p><p>
                Create invoices, register payments and keep track of the discussions with your customers.
              </p>
            </field>
        </record>

        <menuitem id="menu_action_move_out_invoice_type_service"
                  action="action_move_out_invoice_type_service"
                  parent="account.menu_finance_receivables"
                  sequence="1"/>

<!--        Payment Bills-->
        <record id="action_move_in_invoice_type_payment" model="ir.actions.act_window">
            <field name="name">Payment Bills</field>
            <field name="res_model">account.move</field>
            <field name="view_mode">tree,kanban,form,pivot,graph,activity</field>
            <field name="view_id" ref="account.view_invoice_tree"/>
            <field name="search_view_id" ref="account.view_account_invoice_filter"/>
            <field name="domain">[('type', '=', 'in_invoice'), ('journal_id', 'in', (1, 97))]</field>
            <field name="context">{'default_type': 'in_invoice'}</field>
            <field name="help" type="html">
              <p class="o_view_nocontent_smiling_face">
                Create a customer invoice
              </p><p>
                Create invoices, register payments and keep track of the discussions with your customers.
              </p>
            </field>
        </record>

        <menuitem id="menu_action_move_in_invoice_type_payment"
                  action="action_move_in_invoice_type_payment"
                  parent="account.menu_finance_payables"
                  sequence="1"/>

<!--        Receipt Bills-->
        <record id="action_move_in_invoice_type_delivery" model="ir.actions.act_window">
            <field name="name">Receipt Bills</field>
            <field name="res_model">account.move</field>
            <field name="view_mode">tree,kanban,form,pivot,graph,activity</field>
            <field name="view_id" ref="account.view_invoice_tree"/>
            <field name="search_view_id" ref="account.view_account_invoice_filter"/>
            <field name="domain">[('type', '=', 'in_invoice'), ('journal_id', 'in', (117, 119))]</field>
            <field name="context">{'default_type': 'in_invoice'}</field>
            <field name="help" type="html">
              <p class="o_view_nocontent_smiling_face">
                Create a customer invoice
              </p><p>
                Create invoices, register payments and keep track of the discussions with your customers.
              </p>
            </field>
        </record>

        <menuitem id="menu_action_move_in_invoice_type_delivery"
                  action="action_move_in_invoice_type_delivery"
                  parent="account.menu_finance_payables"
                  sequence="1"/>

<!--        Service Bills-->
        <record id="action_move_in_invoice_type_service" model="ir.actions.act_window">
            <field name="name">Service Bills</field>
            <field name="res_model">account.move</field>
            <field name="view_mode">tree,kanban,form,pivot,graph,activity</field>
            <field name="view_id" ref="account.view_invoice_tree"/>
            <field name="search_view_id" ref="account.view_account_invoice_filter"/>
            <field name="domain">[('type', '=', 'in_invoice'), ('journal_id', '=', 118)]</field>
            <field name="context">{'default_type': 'in_invoice'}</field>
            <field name="help" type="html">
              <p class="o_view_nocontent_smiling_face">
                Create a customer invoice
              </p><p>
                Create invoices, register payments and keep track of the discussions with your customers.
              </p>
            </field>
        </record>

        <menuitem id="menu_action_move_in_invoice_type_service"
                  action="action_move_in_invoice_type_service"
                  parent="account.menu_finance_payables"
                  sequence="1"/>
    </data>
</odoo>